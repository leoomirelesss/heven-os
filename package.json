{
  "name": "heven-os",
  "private": true,
  "version": "0.1.0",
  "description": "Hevən OS - Business Operating System for LATAM",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @heven/web & npm run start:dev --workspace @heven/api",
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces",
    "test": "npm run test --workspace @heven/api",
    "format": "prettier --write ."
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
