'use client';
import { useState } from 'react';
export function SimpleCrudScreen({title, seed}:{title:string;seed:string[]}){const [items,setItems]=useState(seed);const [draft,setDraft]=useState('');return <div className='space-y-3'><h2 className='text-2xl font-semibold'>{title}</h2><div className='flex gap-2'><input value={draft} onChange={e=>setDraft(e.target.value)} className='bg-black/20 border border-white/10 p-2 rounded w-full'/><button onClick={()=>{if(draft){setItems([...items,draft]);setDraft('')}}} className='bg-white text-black px-3 rounded'>Add</button></div>{items.map((i,idx)=><div key={i+idx} className='border border-white/10 rounded p-2 flex justify-between'><span>{i}</span><button onClick={()=>setItems(items.filter((_,j)=>j!==idx))}>Delete</button></div>)}</div>}
