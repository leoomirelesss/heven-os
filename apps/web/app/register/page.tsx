'use client';
import { useRouter } from 'next/navigation';import { useState } from 'react';import { signUpWithEmail, signInWithOAuth } from '../../lib/auth';
export default function Register(){const r=useRouter();const [email,setEmail]=useState('');const [password,setPassword]=useState('');return <div className='min-h-screen bg-surface flex items-center justify-center p-6'><div className='w-full max-w-md border border-white/10 bg-panel rounded-xl p-6 space-y-3'><h1 className='text-2xl font-semibold'>Create account</h1><input className='w-full bg-black/20 border border-white/10 p-2 rounded' placeholder='Email' value={email} onChange={e=>setEmail(e.target.value)}/><input type='password' className='w-full bg-black/20 border border-white/10 p-2 rounded' placeholder='Password' value={password} onChange={e=>setPassword(e.target.value)}/><button onClick={async()=>{await signUpWithEmail(email,password);r.push('/onboarding')}} className='w-full bg-white text-black rounded p-2'>Create account</button><button onClick={async()=>{await signInWithOAuth('google');r.push('/onboarding')}} className='w-full border border-white/20 rounded p-2'>Continue with Google</button><button onClick={async()=>{await signInWithOAuth('github');r.push('/onboarding')}} className='w-full border border-white/20 rounded p-2'>Continue with GitHub</button></div></div>}
